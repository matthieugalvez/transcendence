# .env
APP_HOST=
APP_PORT=

# Database Configuration - Use SQLite file path for production
DATABASE_URL=

# Authentication Settings - Generate proper secrets
AUTH_SECRET=
AUTH_SECRET_EXPIRES_IN=
AUTH_REFRESH_SECRET=
AUTH_REFRESH_SECRET_EXPIRES_IN=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Production environment
# Development environment
NODE_ENV=development
PORT=3000
HOST=localhost
BASE_URL=http://localhost:3000
CLIENT_URL=http://localhost:5173
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/oauth2/google/callback

#   NODE_ENV=production
#   PORT=3000
#   HOST=0.0.0.0
#   BASE_URL=https://pong42.click
#   CLIENT_URL=https://pong42.click
#   GOOGLE_REDIRECT_URI=https://pong42.click/api/auth/oauth2/google/callback
#	AVATAR_UPLOAD_DIR=/app/src/server/db/users



# Generate new secrets:
# node -e "console.log('AUTH_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
# node -e "console.log('AUTH_REFRESH_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
