services:
  app:
    build:
      context: .
      dockerfile: configs/Dockerfile
    volumes:
      - /var/.env:/app/.env:ro
    ports:
      - "3000"  # Only expose for internal communication
    environment:
      - AUTH_SECRET=${AUTH_SECRET}
      - AUTH_SECRET_EXPIRES_IN=15m
      - AUTH_REFRESH_SECRET=${AUTH_REFRESH_SECRET}
      - AUTH_REFRESH_SECRET_EXPIRES_IN=24h
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - transcendence

  nginx:
    build:
      context: .
      dockerfile: configs/nginx.Dockerfile
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - transcendence
    volumes:
      - /var/cert/fullchain.pem:/etc/nginx/ssl/cert.pem:ro
      - /var/cert/privkey.pem:/etc/nginx/ssl/key.pem:ro

networks:
  transcendence:
    driver: bridge
